Add a scroll-driven cinematic frame sequence section to my homepage.

It plays a sequence of 240 image frames tied to scroll position (like Apple's product pages), with a sticky canvas, animated word-by-word headline, live frame counter, and progress rail. The whole thing is rAF-throttled and uses imperative DOM updates so it stays buttery smooth at 60fps with zero React re-renders during scroll.

Setup:

Put 240 sequentially-numbered JPG frames in public/scrollreact/ named ezgif-frame-001.jpg through ezgif-frame-240.jpg.
Create the component file src/components/ScrollReactSequence.tsx with the code below.
Add the CSS block to your global stylesheet (e.g. src/index.css).
Import and render <ScrollReactSequence /> on your homepage where you want the cinematic section.
Make sure Tailwind is set up — the component uses Tailwind utility classes (or replace with your own).
Optional: if you want to overlay another section that "slides up" over the sequence at the end, listen for the CSS variables --scrollreact-card (0→1) and --scrollreact-cover (px) on the document root and use them in a transform: translate3d(0, calc(...), 0) on your overlay section.

Use the code below verbatim.

📁 File 1: src/components/ScrollReactSequence.tsx
import React, { useEffect, useRef } from 'react';
const FRAME_COUNT = 240;
// If your app is mounted at a sub-path use import.meta.env.BASE_URL,
// otherwise '/' is fine.
const BASE = import.meta.env.BASE_URL ?? '/';
function framePath(i: number) {
  const n = String(i + 1).padStart(3, '0');
  return `${BASE}scrollreact/ezgif-frame-${n}.jpg`;
}
/** Premium "slow & smooth" easing — slow start, slow finish, expensive feel. */
function easeInOutQuint(t: number) {
  return t < 0.5 ? 16 * t * t * t * t * t : 1 - Math.pow(-2 * t + 2, 5) / 2;
}
export function ScrollReactSequence({
  eyebrow = 'The Experience',
  titleLine1 = 'Every Frame,',
  titleAccent = 'Every Flavor',
}: {
  eyebrow?: string;
  titleLine1?: string;
  titleAccent?: string;
}) {
  const sectionRef = useRef<HTMLDivElement>(null);
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const overlayRef = useRef<HTMLDivElement>(null);
  const headlineRef = useRef<HTMLDivElement>(null);
  const counterRef = useRef<HTMLSpanElement>(null);
  const imagesRef = useRef<HTMLImageElement[]>([]);
  const lastDrawnRef = useRef(-1);
  const targetIdxRef = useRef(0);
  const drawFrame = (idx: number) => {
    const canvas = canvasRef.current;
    if (!canvas) return;
    let pickIdx = -1;
    for (let i = idx; i >= 0; i--) {
      const im = imagesRef.current[i];
      if (im && im.complete && im.naturalWidth > 0) { pickIdx = i; break; }
    }
    if (pickIdx === -1) {
      for (let i = idx + 1; i < FRAME_COUNT; i++) {
        const im = imagesRef.current[i];
        if (im && im.complete && im.naturalWidth > 0) { pickIdx = i; break; }
      }
    }
    if (pickIdx === -1) return;
    const img = imagesRef.current[pickIdx];
    const ctx = canvas.getContext('2d');
    if (!ctx) return;
    const dpr = Math.min(1.25, window.devicePixelRatio || 1);
    const cw = canvas.clientWidth;
    const ch = canvas.clientHeight;
    if (cw === 0 || ch === 0) return;
    const targetW = Math.floor(cw * dpr);
    const targetH = Math.floor(ch * dpr);
    if (canvas.width !== targetW || canvas.height !== targetH) {
      canvas.width = targetW;
      canvas.height = targetH;
    }
    const cr = canvas.width / canvas.height;
    const ir = img.naturalWidth / img.naturalHeight;
    let dw, dh, dx, dy;
    if (ir > cr) {
      dh = canvas.height; dw = dh * ir; dx = (canvas.width - dw) / 2; dy = 0;
    } else {
      dw = canvas.width; dh = dw / ir; dx = 0; dy = (canvas.height - dh) / 2;
    }
    ctx.drawImage(img, dx, dy, dw, dh);
    lastDrawnRef.current = idx;
  };
  // Preload all frames
  useEffect(() => {
    const imgs: HTMLImageElement[] = [];
    for (let i = 0; i < FRAME_COUNT; i++) {
      const img = new Image();
      img.src = framePath(i);
      img.onload = () => {
        const target = targetIdxRef.current;
        if (Math.abs(i - target) <= 5 || lastDrawnRef.current < 0) {
          drawFrame(target);
        }
      };
      imgs.push(img);
    }
    imagesRef.current = imgs;
  }, []);
  useEffect(() => {
    let raf = 0;
    let scheduled = false;
    let lastIdx = -1;
    let lastVisible: boolean | null = null;
    const root = document.documentElement;
    const headline = headlineRef.current;
    const counter = counterRef.current;
    const tick = () => {
      scheduled = false;
      const sec = sectionRef.current;
      const overlay = overlayRef.current;
      if (!sec || !overlay) return;
      const rect = sec.getBoundingClientRect();
      const total = sec.offsetHeight;
      // Card slide-up progress (0→1) — exposed as CSS var so any
      // overlay section can ride on top of the sequence.
      let cp: number;
      if (rect.top > 0) cp = 0;
      else if (rect.bottom <= 0) cp = 1;
      else {
        const cardStart = total * 0.60;
        const cardEnd = total * 0.96;
        const raw = Math.min(1, Math.max(0, (-rect.top - cardStart) / Math.max(1, cardEnd - cardStart)));
        cp = easeInOutQuint(raw);
      }
      root.style.setProperty('--scrollreact-card', String(cp));
      const cover = Math.max(0, Math.round(rect.bottom));
      root.style.setProperty('--scrollreact-cover', `${cover}px`);
      // Toggle overlay visibility imperatively (no React re-render)
      const inRange = rect.top <= 0 && rect.bottom > 0;
      if (inRange !== lastVisible) {
        overlay.style.opacity = inRange ? '1' : '0';
        overlay.style.visibility = inRange ? 'visible' : 'hidden';
        lastVisible = inRange;
      }
      if (!inRange) return;
      // Frame index — frames play over the first 58% of section scroll
      const playRange = total * 0.58;
      const progress = Math.min(1, Math.max(0, -rect.top / Math.max(1, playRange)));
      const idx = Math.min(FRAME_COUNT - 1, Math.max(0, Math.round(progress * (FRAME_COUNT - 1))));
      targetIdxRef.current = idx;
      if (idx !== lastIdx) {
        lastIdx = idx;
        drawFrame(idx);
        if (counter) counter.textContent = String(idx + 1).padStart(3, '0');
      }
      // Cinematic text-effect vars consumed by .sr-* classes
      if (headline) {
        const textOut = Math.min(1, Math.max(0, (progress - 0.75) / 0.25));
        headline.style.setProperty('--sr-progress', progress.toFixed(3));
        headline.style.setProperty('--sr-textOut', textOut.toFixed(3));
      }
    };
    const onScroll = () => {
      if (scheduled) return;
      scheduled = true;
      raf = requestAnimationFrame(tick);
    };
    const onResize = () => { lastIdx = -1; onScroll(); };
    window.addEventListener('scroll', onScroll, { passive: true });
    window.addEventListener('resize', onResize);
    onScroll();
    return () => {
      window.removeEventListener('scroll', onScroll);
      window.removeEventListener('resize', onResize);
      cancelAnimationFrame(raf);
    };
  }, []);
  return (
    <section
      ref={sectionRef}
      className="relative bg-black"
      style={{ height: '320vh' }}
    >
      {/* Static first frame so the section looks alive before scroll lock */}
      <div className="absolute top-0 left-0 w-full h-screen overflow-hidden">
        <img
          src={framePath(0)}
          alt=""
          aria-hidden="true"
          className="absolute inset-0 w-full h-full object-cover"
        />
        <div className="absolute inset-0 bg-gradient-to-b from-black/35 via-black/10 to-black/70" />
        <div className="absolute inset-x-0 top-0 pt-24 px-6 lg:px-12 flex flex-col items-center gap-4">
          <span className="px-4 py-1.5 rounded-full bg-black/55 backdrop-blur-md border border-white/15 text-[11px] uppercase tracking-[0.35em] text-amber-300">
            {eyebrow}
          </span>
          <h2
            className="text-5xl md:text-7xl lg:text-8xl font-black tracking-tight text-white max-w-4xl text-center leading-[0.95]"
            style={{ textShadow: '0 6px 30px rgba(0,0,0,0.85)' }}
          >
            {titleLine1} <span className="text-orange-500">{titleAccent}</span>
          </h2>
        </div>
      </div>
      {/* Sticky canvas overlay — fixed-position, opacity toggled imperatively */}
      <div
        ref={overlayRef}
        className="fixed inset-0 z-30 pointer-events-none"
        style={{ opacity: 0, visibility: 'hidden', willChange: 'opacity', transform: 'translateZ(0)' }}
      >
        <canvas ref={canvasRef} className="absolute inset-0 w-full h-full bg-black" />
        <div className="absolute inset-0 bg-gradient-to-b from-black/35 via-black/10 to-black/70" />
        {/* Animated headline driven by --sr-progress */}
        <div className="absolute inset-x-0 top-0 pt-24 px-6 lg:px-12 flex flex-col items-center">
          <div ref={headlineRef} className="sr-headline max-w-4xl text-center">
            <span className="sr-eyebrow px-4 py-1.5 rounded-full bg-black/55 backdrop-blur-md border border-white/15 text-[11px] uppercase font-bold text-amber-300">
              {eyebrow}
            </span>
            <h2
              className="font-black tracking-tight text-white leading-[0.95] mt-5 text-5xl md:text-7xl lg:text-8xl"
              style={{ textShadow: '0 6px 30px rgba(0,0,0,0.85)' }}
            >
              <AnimatedHeadline line1={titleLine1} accent={titleAccent} />
            </h2>
          </div>
        </div>
        {/* Frame counter + progress rail */}
        <div className="sr-meta">
          <div className="sr-counter">
            <span ref={counterRef} className="sr-counter-current">001</span>
            <span className="sr-counter-divider">/</span>
            <span className="sr-counter-total">{String(FRAME_COUNT).padStart(3, '0')}</span>
          </div>
          <div className="sr-rail" aria-hidden="true" />
        </div>
      </div>
    </section>
  );
}
function AnimatedHeadline({ line1, accent }: { line1: string; accent: string }) {
  const words1 = line1.trim().split(/\s+/).filter(Boolean);
  const words2 = accent.trim().split(/\s+/).filter(Boolean);
  let i = 0;
  return (
    <span className="sr-line">
      {words1.map((w) => (
        <span key={`a-${i}-${w}`} className="sr-word" style={{ ['--i' as any]: i++ }}>{w}</span>
      ))}
      {words2.map((w) => (
        <span key={`b-${i}-${w}`} className="sr-word sr-word--accent" style={{ ['--i' as any]: i++ }}>{w}</span>
      ))}
    </span>
  );
}

📁 File 2: append to src/index.css (or your global CSS)
/* ─────────────────────────────────────────────────────────
   ScrollReactSequence — cinematic text effects
   Driven by --sr-progress 0→1 and --sr-textOut 0→1, set
   imperatively by the component on each rAF tick.
   Pure CSS so they cost zero React renders during scroll.
   ───────────────────────────────────────────────────────── */
.sr-headline {
  --sr-progress: 0;
  --sr-textOut: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.05em;
  transform-origin: 50% 50%;
  transform: translate3d(
      0,
      calc(var(--sr-progress) * -22px + var(--sr-textOut) * -40px),
      0
    )
    scale(calc(1 + var(--sr-progress) * 0.04 + var(--sr-textOut) * 0.06));
  opacity: calc(1 - var(--sr-textOut));
  filter: blur(calc(var(--sr-textOut) * 8px));
  will-change: transform, opacity, filter;
}
.sr-line {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 0 0.28em;
  line-height: 0.95;
}
.sr-word {
  display: inline-block;
  --sr-rev: clamp(0, (var(--sr-progress) - var(--i, 0) * 0.05) * 12, 1);
  transform: translate3d(0, calc((1 - var(--sr-rev)) * 70px), 0);
  opacity: var(--sr-rev);
  filter: blur(calc((1 - var(--sr-rev)) * 14px));
  will-change: transform, opacity, filter;
}
.sr-word--accent {
  color: #f4a345; /* swap for your brand orange */
  text-shadow:
    0 6px 30px rgba(0, 0, 0, 0.85),
    0 0 calc(var(--sr-progress) * 40px) rgba(244, 163, 69, calc(var(--sr-progress) * 0.55));
}
.sr-eyebrow {
  --sr-rev: clamp(0, var(--sr-progress) * 16, 1);
  transform: translate3d(0, calc((1 - var(--sr-rev)) * -24px), 0);
  opacity: calc(var(--sr-rev) * (1 - var(--sr-textOut)));
  letter-spacing: calc(0.35em + var(--sr-progress) * 0.05em);
  transition: letter-spacing 120ms linear;
}
.sr-meta {
  position: absolute;
  inset: auto 0 6vh 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 14px;
  padding: 0 24px;
  opacity: calc(clamp(0, var(--sr-progress) * 8, 1) * (1 - var(--sr-textOut)));
  transition: opacity 200ms linear;
  pointer-events: none;
}
.sr-counter {
  display: inline-flex;
  align-items: baseline;
  gap: 8px;
  font-variant-numeric: tabular-nums;
  font-weight: 900;
  letter-spacing: 0.15em;
  color: rgba(255, 255, 255, 0.92);
  text-shadow: 0 4px 18px rgba(0, 0, 0, 0.7);
}
.sr-counter-current {
  font-size: 1.05rem;
  color: #f4a345; /* brand accent */
}
.sr-counter-divider,
.sr-counter-total {
  font-size: 0.7rem;
  color: rgba(255, 255, 255, 0.55);
}
.sr-rail {
  position: relative;
  width: min(360px, 60vw);
  height: 2px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.12);
  overflow: hidden;
}
.sr-rail::after {
  content: '';
  position: absolute;
  inset: 0 auto 0 0;
  width: calc(var(--sr-progress) * 100%);
  background: linear-gradient(90deg, #d4a437, #f4a345);
  border-radius: 999px;
  box-shadow: 0 0 14px rgba(244, 163, 69, 0.6);
  transition: none;
}
@media (prefers-reduced-motion: reduce) {
  .sr-headline,
  .sr-word,
  .sr-eyebrow {
    transform: none !important;
    filter: none !important;
    opacity: 1 !important;
  }
}

📁 Usage in your homepage
import { ScrollReactSequence } from '@/components/ScrollReactSequence';
export default function Home() {
  return (
    <main>
      {/* …your sections above… */}
      <ScrollReactSequence
        eyebrow="The Experience"
        titleLine1="Every Frame,"
        titleAccent="Every Flavor"
      />
      {/* …your sections below… */}
    </main>
  );
}

🎬 How to make your frames
If you have a video and want 240 frames out of it, run this once locally:

ffmpeg -i input.mp4 -vf "fps=24,scale=1280:-2" -q:v 4 public/scrollreact/ezgif-frame-%03d.jpg

Adjust fps=24 until you get ~240 frames. Aim for ~80–150 KB per JPG for fast preload.

🛠 Tweaking knobs (inside the component)
What you want	Edit
Make it slower / smoother	Increase height: '320vh' to 400vh
Different number of frames	Change FRAME_COUNT (and rename your files)
Frames finish earlier/later	Change total * 0.58 (the playRange)
Card slide range	Change cardStart = total * 0.60 and cardEnd = total * 0.96
Less DPR cost on retina	Lower Math.min(1.25, …) to 1.0